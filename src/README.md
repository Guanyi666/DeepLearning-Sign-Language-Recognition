基于双流 CNN-LSTM 融合网络的手语识别系统

Sign Language Recognition System Based on Dual-Stream CNN-LSTM

🛠️ 环境依赖 (Requirements)

运行本项目前，请确保已安装以下核心依赖库：

Python 3.8+

TensorFlow 2.x

protobuf 5.28.3 (tensorflow依赖)

MediaPipe (用于手部关键点检测)

OpenCV (cv2)

NumPy

Pillow (用于在视频流中绘制中文)


🚀 运行

本项目主要包含三个核心功能模块

1. 启动实时手语识别系统 (最终展示)

项目的核心展示程序，调用摄像头进行实时手语检测、识别与翻译。

功能：开启摄像头 -> 多线程捕获 -> 双流模型预测 -> 自然语言翻译 -> 界面显示。

运行realtime_detect_final.py：

python src/Camera/realtime_detect_final.py


操作说明：

保持手部出现在摄像头画面中。

做完动作后，系统会自动识别并显示单词。

手部离开或静止约 2 秒后，系统会自动将单词序列翻译为中文句子。

按 q 键退出程序。

2. 模型训练 (Training)

如果重新训练模型，或验证训练过程，请运行此脚本。

功能：读取处理好的特征文件 (.npy)，训练双流 CNN-LSTM 模型，并保存为 .h5 文件。

运行run.py：

python src/Conv1D/run.py


输出：训练好的模型将保存在 src1/Conv1D/ 目录下（如 sign_language_model_dual.h5），并生成训练过程的 Loss/Accuracy 曲线图。

3. 原始数据预处理 (Data Processing)

用于将采集的原始视频转化为模型可读取的特征向量。

功能：读取原始视频 -> MediaPipe 提取关键点 -> 能量分割动作 -> 统一序列长度 -> 保存为 .npy。

运行process_repeat_action_video.py：

python src/process_video/process_repeat_action_video.py


